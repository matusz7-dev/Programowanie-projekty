#include "ships.h"
#include "tictactoe.h"

#include <iostream>

using namespace std;

int main()
{

    switch

    case 1
    ships::initialize();
    ships::playGame();

    case 2
    tictactoe::playGame();

    // FGET -> MODE
    // SWITCH MODE
    // MODE 1 -> SHIPS::start()
    // MODE 2 -> TIC TAC TOE::start()

    // wybierz gre
    // 1. statki
    // 2. kolko i krzyzyk

    return EXIT_SUCCESS;
}